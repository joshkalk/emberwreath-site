# AGENTS.md

## Project context
- This is an Eleventy static site for a player-facing D&D campaign setting called Emberwreath.
- Do not invent new lore.
- Do not add GM-only secrets to public pages.
- Do not rewrite long PHB entries unless explicitly asked.
- Keep player-facing pages concise and useful for character creation.

## Site conventions
- Use existing Eleventy layout conventions.
- Use Eleventy URL filters for internal links.
- Preserve the navigation structure unless explicitly asked to change it.

## Workflow
- Run `npm run build` before finishing any task.
- Do not commit generated `_site/` or `node_modules/`.
